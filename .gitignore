# Godot-specific ignores
.import/

# These files are excluded by default because they can contain credentials and other sensitive
# information, but otherwise may be useful in a future scenario where credentials are stored
# separately, for example: https://github.com/godotengine/godot/pull/35930
# You can remove these rules and commit these files if you wish to share them, but be careful!
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/

# macOS
.DS_Store
__MACOSX
